# C-055 — фактическая установка в production

Дата: 2026-09-17.

- PR #95 объединён в `main` коммитом `a38f8af915d09ffccf3a13b3897f2855ad0906c1`.
- GitHub Safety успешно выполнил Node-, SQL-, A1 fault-injection, replay и Chromium/WebKit-проверки.
- GitHub Pages опубликован; `reestr.html` отвечает HTTP 200 и содержит актуальный маршрут передачи Word.
- Перед изменением базы активных заданий `queued/running` не было.
- Миграция `studkab_preparation_diagnostics` применена. Добавлены четыре диагностических поля; `studkab_gen_fail_preparation` доступна для выполнения только `service_role`.
- Контрольные количества до и после совпали: 5 заявок, 6 облачных записей, 3 подписки, 5 заданий, 54 части.
- `studkab-generation` опубликована как v25, `studkab-generation-api` — как v30. Обе ACTIVE и сохраняют `verify_jwt=true`.
- Анонимный probe обеих функций вернул HTTP 401.
- Локальный итоговый набор: 255/255 тестов; `git diff --check` без замечаний.
- Платный AI-провайдер не вызывался.

Открытые пункты не маскируются этой установкой: реальная A1-приёмка, восстановление полного backup, загрузка материалов студентом и уведомление о готовности результата остаются отдельными этапами.
